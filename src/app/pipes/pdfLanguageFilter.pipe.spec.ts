
import { PdfLanguageFilterPipe } from './pdfLanguageFilter.pipe';

describe('Pipe: PdfLanguageFiltere', () => {
  it('create an instance', () => {
    let pipe = new PdfLanguageFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
