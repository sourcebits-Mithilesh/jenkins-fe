import { PatterncheckPipe } from './patterncheck.pipe';

describe('PatterncheckPipe', () => {
  it('create an instance', () => {
    const pipe = new PatterncheckPipe();
    expect(pipe).toBeTruthy();
  });
});
